Build a small full-stack web app called “Emoji Combos – Free Emoji Combination Generator”.
Tech stack
Backend: Node.js + Express
Frontend: HTML, CSS, vanilla JavaScript (no React)
Styling: Tailwind CSS via CDN
Data: Static JSON file for emoji combos (no database needed)
High-level concept
This app is a single-tool microsite for the keyword “emoji combos”. It lets users:
Browse curated emoji combinations
Filter by category (cute, aesthetic, funny, flirty, angry, meme, etc.)
Search by text (e.g. “heart”, “happy”, “eyes”)
Copy any combo with one click
Generate a random combo
This should be structured so the entire app can later be embedded under a route like /tools/emoji-combos on a larger site.
Routes
GET / → Redirect to /emoji-combos
GET /emoji-combos → Main HTML page
GET /api/emoji-combos → Returns JSON list of emoji combos
Emoji data
Create a file emojiCombos.json with an array of objects like:
[
  {
    "id": 1,
    "combo": "✨💫🌙",
    "label": "Dreamy night",
    "category": "aesthetic",
    "tags": ["night", "dreamy", "aesthetic"]
  },
  {
    "id": 2,
    "combo": "💅✨",
    "label": "Doing the most",
    "category": "sass",
    "tags": ["sass", "attitude"]
  }
]
Seed at least 80–100 combos across categories:
cute
aesthetic
funny
flirty
meme
angry
sad
happy
spooky
celebratory
The backend should just read this file and serve it via /api/emoji-combos.
Frontend layout (single page)
Use a centered, mobile-first layout with Tailwind:
Header
Big title: “Emoji Combos Generator”
Subtitle: “Copy-paste aesthetic emoji combos for your captions, bios, and posts.”
Small line under it: “Free tool for creators.”
Controls section (top of page)
Search input: placeholder “Search emojis or names (e.g. ‘heart’, ‘happy’)”
Category filter: horizontal scrollable list of pill buttons for categories:
“All”, “Cute”, “Aesthetic”, “Funny”, “Flirty”, “Meme”, “Happy”, “Sad”, “Angry”, “Spooky”, “Celebration”
“Random Combo” button on the right side or below on mobile
Combos grid
Responsive grid using Tailwind (grid-cols-1 sm:grid-cols-2 md:grid-cols-3).
Each combo card shows:
Big emoji combo (large font)
Small label text under it
Category badge (tiny pill in the corner)
A “Copy” button
Copy behavior
When user clicks “Copy”:
Copy the emoji combo string to clipboard
Brief toast/notification at the bottom: “Copied to clipboard ✅”
Use navigator.clipboard.writeText in JS
Random combo behavior
Clicking “Random Combo”:
Select a random item from the currently filtered list (respect search + category)
Scroll it into view and briefly highlight the card (e.g. with a temporary ring animation or background flash)
Frontend logic
In public/main.js or similar:
On page load, fetch /api/emoji-combos
Store them in memory
Render combos into the grid
Implement:
Text search (case-insensitive; match combo, label, or tags)
Category filtering
Combined filter (search + category)
Copy to clipboard
Random combo picker
Make sure filtering is done entirely client-side for speed.
SEO & meta
On /emoji-combos HTML:
<title>Emoji Combos Generator – Aesthetic Emoji Combinations for Captions & Bios</title>
<meta name="description" content="Generate and copy free emoji combos for TikTok, Instagram, YouTube, and more. Browse aesthetic, cute, funny, and meme emoji combinations.">
Use a clean H1: “Emoji Combos Generator”
Include a short text section under the header (2–3 paragraphs) explaining:
what emoji combos are
how to use them in captions, bios, and messages
that this is a free tool for creators
Design guidelines
Background: light neutral (bg-slate-50) with cards on white.
Cards: rounded (rounded-xl), subtle shadow.
Primary accent color: use Tailwind’s indigo or violet scale for buttons and category pills.
Buttons: large, rounded-full, with hover states.
Make sure it looks great on mobile first.
Extras
Add a small footer:
“Built for creators. Add this to your bookmarks.”
Placeholder link area where a future CTA (e.g. “Explore more creator tools”) can go.
Use simple, clean fonts (Tailwind default is fine).
The code should be clean and modular, with:
server.js (Express setup + routes)
public/index.html (main page for /emoji-combos)
public/main.js (frontend logic)
public/styles.css (if needed, but mostly Tailwind classes)
emojiCombos.json (data)
Make sure the app runs with node server.js and listens on process.env.PORT || 3000.