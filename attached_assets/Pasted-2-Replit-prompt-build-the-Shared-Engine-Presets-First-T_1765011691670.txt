2. Replit prompt: build the Shared Engine + Presets + First Tool
Here’s what to paste into Replit (or adapt slightly if your repo already exists).
This assumes:
Next.js (App Router)
TypeScript
Tailwind
Using sharp on the backend for image processing
🔧 Master Replit Prompt (Shared Engine + YouTube Thumbnail Resizer)
You are coding inside a Next.js (App Router, TypeScript) project with Tailwind configured.
Build a shared image resizing engine plus a YouTube Thumbnail Resizer page, using this architecture:
Shared client component: components/PlatformImageToolClient.tsx
Presets registry: data/platformImagePresets.ts
API route for processing: app/api/platform-image/resize/route.ts
Thin page shell for each tool: e.g. app/tools/youtube-thumbnail-resizer/page.tsx
Core Requirements
data/platformImagePresets.ts
Export a PlatformImagePreset TypeScript type:
export type PlatformImagePreset = {
  id: string;            // e.g. "youtube-thumbnail"
  slug: string;          // e.g. "youtube-thumbnail-resizer"
  name: string;          // Human label
  width: number;
  height: number;
  aspectRatioLabel: string;  // "16:9", "9:16", etc.
  maxFileSizeMB: number;
  outputFormats: ("jpg" | "png")[];
  seo: {
    title: string;
    description: string;
    h1: string;
    faq: { question: string; answer: string }[];
  };
  ui: {
    showSafeZoneOverlay?: boolean;
    notes?: string[];
  };
};
Export:
export const platformImagePresets: PlatformImagePreset[] = [ ... ];
export function getPresetBySlug(slug: string): PlatformImagePreset | undefined { ... }
export function getPresetById(id: string): PlatformImagePreset | undefined { ... }
Add at least these presets:
youtube-thumbnail (slug: youtube-thumbnail-resizer, 1280×720, aspect 16:9)
instagram-post (slug: instagram-post-resizer, with 1080×1080 as the primary size for now)
instagram-story (slug: instagram-story-resizer, 1080×1920)
tiktok-cover (slug: tiktok-video-resizer, 1080×1920)
twitter-header (slug: twitter-header-resizer, 1500×500)
linkedin-banner (slug: linkedin-banner-resizer, 1584×396)
Fill reasonable SEO text (title, description, h1, FAQ) for the YouTube Thumbnail preset; you can stub simple text for the others.
API route: app/api/platform-image/resize/route.ts
Use sharp for image processing.
Accept a POST with:
a presetId field (string)
one uploaded image file (Field name: file)
Use formData = await request.formData() to read the file in the Next.js route handler.
Look up the preset via getPresetById(presetId).
Validate:
File is present
File size <= maxFileSizeMB
Process:
sharp(inputBuffer)
  .resize(preset.width, preset.height, { fit: "cover" })
  .jpeg({ quality: 90 })
Return a Response with:
Content-Type: image/jpeg
The resized image as the body
On error, return JSON with { error: string } and a non-200 status.
Shared client component: components/PlatformImageToolClient.tsx
Mark it "use client".
Props:
type PlatformImageToolClientProps = {
  preset: PlatformImagePreset;
};
Behavior:
Render:
Tool title (from preset.seo.h1)
Subheading with preset.width × preset.height and preset.aspectRatioLabel
File input
Optional notes (preset.ui.notes)
If preset.ui.showSafeZoneOverlay is true, render a visual overlay on top of the preview (e.g. a semi-transparent box representing a safe text zone).
When user selects a file:
Show original preview via URL.createObjectURL(file).
On “Resize Image” button click:
POST to /api/platform-image/resize using fetch and FormData.
Include preset.id in the form data.
Read the binary response and create a downloadable blob URL.
Render:
Original preview
Resized preview (using the returned blob URL)
A “Download Resized Image” button (anchor with download attribute pointing to blob URL).
Show loading and basic error states.
Use Tailwind for layout:
Two-column layout on desktop (original vs resized)
Stack vertically on mobile.
Page shell: app/tools/youtube-thumbnail-resizer/page.tsx
Use App Router with TypeScript.
Import getPresetBySlug and PlatformImageToolClient.
Resolve the preset for "youtube-thumbnail-resizer".
If missing, throw an error (but it should exist).
Export metadata using the preset’s seo fields:
export const metadata: Metadata = {
  title: preset.seo.title,
  description: preset.seo.description,
};
Default export a React component:
export default function Page() {
  const preset = getPresetBySlug("youtube-thumbnail-resizer")!;
  return (
    <main className="min-h-screen px-4 py-10 max-w-5xl mx-auto">
      <PlatformImageToolClient preset={preset} />
      {/* Optional: FAQ section using preset.seo.faq */}
    </main>
  );
}
Under the main tool UI, render a simple FAQ using preset.seo.faq.
Stub additional pages that reuse the engine
Create these thin pages that reuse the same client + presets:
app/tools/instagram-post-resizer/page.tsx
app/tools/instagram-story-resizer/page.tsx
app/tools/tiktok-video-resizer/page.tsx
app/tools/twitter-header-resizer/page.tsx
app/tools/linkedin-banner-resizer/page.tsx
Each one:
Uses getPresetBySlug("<slug>")
Exports metadata from the preset
Renders <PlatformImageToolClient preset={preset} />
It’s fine if their SEO copy is simpler for now; the YouTube one should be solid.
General expectations
All code must compile with TypeScript.
Use only built-in Next.js/React APIs and sharp.
Handle errors defensively: missing preset, bad file, oversized file.
Assume this will later be integrated into a larger SEO site, so keep:
Clean semantic HTML
No inline styles; use Tailwind classes
Clear headings (H1, H2, etc.)
Implement all these files and wire everything so that visiting /tools/youtube-thumbnail-resizer renders a working YouTube Thumbnail Resizer, powered by the shared engine and presets.
3. How you’ll expand this later
Once this is in:
Adding “Pinterest Pin Resizer” is literally:
Add a new preset in platformImagePresets.ts
Add a thin page app/tools/pinterest-pin-resizer/page.tsx
The engine never changes, unless you want a new capability (watermarks, multi-format download, etc.)
That’s exactly how you want a “tool factory” to behave.