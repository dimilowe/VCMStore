Step 1 – Fix .text-gradient in globals.css (ONE change, ONE file)
You want:
.text-gradient to only apply in marketing contexts (hero headlines, landing pages)
Children not inheriting transparent text fill
Architecturally-correct approach:
Identify the marketing wrapper class used on your landing/marketing pages
(something like .marketing-page, .marketing-shell, .site-hero, whatever your layout uses).
In app/globals.css, change your gradient definition from a global:
.text-gradient {
  /* gradient styles */
}
to something like:
/* inside @layer utilities, where it already lives */
@layer utilities {
  .marketing-page .text-gradient {
    background-image: linear-gradient(90deg, #ec4899, #f97316);
    -webkit-background-clip: text;
    color: transparent;
    -webkit-text-fill-color: transparent;
  }

  /* ensure children of gradient text don’t inherit transparent fill */
  .marketing-page .text-gradient * {
    -webkit-text-fill-color: currentColor;
    color: inherit;
  }
}
Then make sure tool pages do not use .marketing-page as a wrapper. That alone should stop the bleed into tools entirely.
Atomic Replit prompt for this (you can paste this as-is)
Goal: Scope the .text-gradient utility so it only affects marketing sections and cannot affect tool pages.
Do not add inline styles anywhere. Do not touch any engine components or layouts other than adjusting the gradient selector in app/globals.css.
Open app/globals.css. Find the current .text-gradient definition (it should be inside @layer utilities).
Replace it so that:
The gradient only applies when .text-gradient is inside a marketing wrapper (for example .marketing-page .text-gradient).
Children of .text-gradient don’t inherit -webkit-text-fill-color: transparent.
For example, it should look roughly like:
@layer utilities {
  .marketing-page .text-gradient {
    background-image: linear-gradient(90deg, #ec4899, #f97316);
    -webkit-background-clip: text;
    color: transparent;
    -webkit-text-fill-color: transparent;
  }

  .marketing-page .text-gradient * {
    -webkit-text-fill-color: currentColor;
    color: inherit;
  }
}
If we don’t already have a consistent marketing wrapper class, use whatever wrapper class is applied on marketing/landing pages and do not wrap tool pages with it.
Show me the exact diff for app/globals.css and any marketing layout file you changed. Do not modify any engine components or tool layouts in this step.
Run that, nothing else.